{
  "name": "@nqdrive/storage",
  "version": "0.1.0",
  "private": true,
  "description": "Storage Provider abstraction layer. Defines the StorageProvider interface and concrete implementations (Google Drive, and future providers like R2, S3, B2, Wasabi, Dropbox, OneDrive, MinIO).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nqdrive/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
