{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "types": ["@cloudflare/workers-types"],
    "noEmit": true
  },
  "include": [
    "src"
    // "worker-configuration.d.ts" — generated by `wrangler types`, uncomment after running it.
    // Run: pnpm --filter @nqdrive/worker cf-typegen
  ]
}
